import { DesktopOnlyDirective } from './desktop-only.directive';

describe('DesktopOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new DesktopOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
