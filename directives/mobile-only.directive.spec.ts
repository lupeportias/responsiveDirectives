import { MobileOnlyDirective } from './mobile-only.directive';

describe('MobileOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new MobileOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
